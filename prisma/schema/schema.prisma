generator client {
  provider = "prisma-client"
  output   = "../../src/generated/client"
}

datasource db {
  provider = "postgresql"
}

model User {
  id    String  @default(uuid())
  email String  @unique
  name  String?
}
